"use client";
import { useEffect } from "react";

export default function DebugFilterOptions({ filterOptions }: { filterOptions: any }) {
  // intentionally left blank — debug helper retained but silenced
  return null;
}
